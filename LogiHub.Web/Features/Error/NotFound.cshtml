@{ ViewData["Title"] = "Ubicazione non trovata"; }

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="card border-0 shadow-sm p-5 border-top border-5 border-primary">
                <div class="card-body">
                    <div class="mb-4 text-muted opacity-25">
                        <i class="fa-solid fa-box-open fa-5x"></i>
                    </div>
                    <h1 class="display-6 fw-bold text-primary">Ubicazione Vuota</h1>
                    <p class="lead mb-4">
                        Il percorso richiesto non risulta mappato nel sistema logistico (Errore 404).
                    </p>
                    <p class="text-muted small mb-4">
                        La pagina potrebbe essere stata spostata o il link inserito non è corretto.
                    </p>
                    <a href="@Url.Action(MVC.Magazzino.SemiLavorati.Index())" class="btn btn-primary px-4 shadow-sm">
                        <i class="fa-solid fa-arrow-left me-2"></i>Torna alla Gestione
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>